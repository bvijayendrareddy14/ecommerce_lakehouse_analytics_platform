# This is a Databricks asset bundle definition for commerce_config_2.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: commerce_config_2
  uuid: 79ec0450-b585-4b19-9ce8-967211831201

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405618330413294.14.azuredatabricks.net
    variables:
      catalog: commerce_stage_dev
      schema: silver
  prod:
    mode: production
    workspace:
      host: https://adb-7405618330413294.14.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/meghanath777@outlook.com to make sure we only have a single copy.
      root_path: /Workspace/Users/meghanath777@outlook.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: commerce_stage_prod
      schema: silver
